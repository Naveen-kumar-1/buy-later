{
  "name": "buy-later-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "postinstall": "npm run postinstall --prefix server || echo 'Prisma not installed, skipping client generation'"
  }
}
